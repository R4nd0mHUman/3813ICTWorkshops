# Angular client
Create the Angular scaffold in this folder with `ng new client --routing --style=css` (or copy these src files into a generated Angular project), then run `ng serve`.
This has the three required pages: Products, Add Product, and Update Product.
